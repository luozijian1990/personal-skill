# Proposal: {{title}}

## Why

<!--
一句话讲清业务驱动力。回答"为什么现在要做这件事"。
聚焦：外部变化（平台政策、客户需求、合规要求）或内部痛点（性能瓶颈、技术债、故障频发）。
-->

{{business_driver}}

## What Changes

<!--
变更清单，粒度到文件/模块级别。每条标注 新增/修改/删除。
读者看完这一节应该知道"改了哪些东西"，但不需要知道"为什么这样改"（那是 design.md 的事）。
-->

- **新增** `{{NewClass}}` — {{一句话描述职责}}
- **新增** `{{EnumOrConfig}}` — {{枚举/配置变更描述}}
- **修改** `{{ExistingClass}}` — {{改动摘要，如"重写 N 个方法，注入 V2 handler"}}
- **前端{{改动/不改动}}**：{{描述前端影响}}
- **授权/业务流程**：{{端到端流程简述，如"前端传入 X → 后端做 Y → 写入 Z → 注册定时任务"}}
- {{兜底说明，如"原有 V1 店铺完全不受影响"}}

## Capabilities

<!--
用命名的方式列出这次交付的能力单元。
每个 capability 是一个可独立描述、可独立验证的功能点。
方便后续写测试用例和 release notes。
-->

### New Capabilities

- `{{capability-id}}`: {{能力描述}}

### Modified Capabilities

- `{{capability-id}}`: {{改动描述}}
<!-- 如果没有修改的能力，写"（无）" -->

## Impact

<!--
影响面分析。给 reviewer 和运维看。
逐维度列出，没有影响的维度可以删除。
-->

- **新增文件**: {{文件列表}}
- **修改文件**: {{文件列表 + 改动摘要}}
- **数据库**: {{schema 变更 / 数据写入变更 / "无 schema 变更"}}
- **缓存（Redis/本地）**: {{key 结构、过期策略}}
- **外部依赖**: {{第三方 API、SDK 版本}}
- **配置变更**: {{配置项、环境变量}}
- **影响范围**: {{对上下游服务的影响}}
