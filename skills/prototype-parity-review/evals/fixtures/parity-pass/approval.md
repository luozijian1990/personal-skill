# 已批准的比较范围

该 HTML Prototype 已批准作为视觉与交互基准。

请在 fixture 表示的桌面布局下审查以下流程：

1. 在“处理中”和“已解决”事件 Tab 之间切换。
2. 打开事件详情 Drawer。
3. 指派事件并观察成功反馈。
4. 验证 Tab 没有事件时的空状态。

React 实现可以使用组件，但必须保留原型的冷白布局、紧凑密度、直角几何、顶部导航、文案和可观察行为。
