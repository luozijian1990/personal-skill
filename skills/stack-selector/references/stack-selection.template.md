# 选型确认单：{{project_name}}

<!--
本模板由 stack-selector skill 产出，落盘路径：
  docs/plans/YYYY-MM-DD-<topic>-stack-selection.md
  docs/plans/YYYY-MM-DD-<topic>-stack-selection.draft.md（形态、语言或关键开放项未确认时）

使用约定：
- 占位符 {{...}} 全部替换为实际内容。
- 没有内容的小节写「（无）」，不要整节删除——保留空节让 SDD 知道你确实考虑过。
- 「显式排除」是这份单子的灵魂，绝不能省。
- 每条理由必须落到这个项目的实际需要，不要写「业界主流 / 性能好 / 生态成熟」这种空话。
- 项目形态、开发语言和关键开放项全部确认后，才写最终确认单；否则写 draft 草案。
-->

## 一、项目识别

<!--
- 形态若是从 mock 推断的，标注「（推断，待确认）」。
- 语言若是按规则建议的，标注「（建议，待确认）」+ 一句话理由。
- 核心能力清单来自第一步「解析 mock」，是后面所有选型的依据。
- 任何「待确认」项都必须同步列入第五节，不能只在本节标注。
-->

- **项目形态**：{{backend / 采集执行 agent / CLI}}{{（推断，待确认）/ 留空}}
- **开发语言**：{{Python / Golang}}{{（建议，待确认）—— 一句话理由 / 留空}}
- **从 mock 反推出的核心能力**：{{A、B、C…}}

## 二、核心选型

<!--
逐个核心维度列：维度 → 选定技术 → 一句话理由。
backend 形态至少覆盖：Web 框架、ORM/数据访问、数据库、配置管理、日志、认证。
agent / CLI 形态按 references/stack-matrix.md 对应分支的核心维度填，可删除用不到的行。
-->

- **Web 框架**：{{选定技术}} —— {{一句话理由}}
- **ORM / 数据访问**：{{选定技术}} —— {{一句话理由}}
- **数据库**：{{选定技术}} —— {{一句话理由}}
- **配置管理**：{{选定技术}} —— {{一句话理由}}
- **日志**：{{选定技术}} —— {{一句话理由}}
- **认证**：{{选定技术}} —— {{一句话理由}}

## 三、按需组件（已选用）

<!--
只列「在 mock 里找到信号、确实要引入」的组件。
每条必须四要素齐全：组件类别 → 选定技术 → 触发它的 mock 信号 → 一句话理由。
没有触发信号 → 不许出现在这一节，移到第四节「显式排除」。
没有按需组件 → 写「（无——核心选型已覆盖本项目需求）」。
-->

- **{{组件类别，如 缓存 / 消息 / 服务端推送 / 时序库 / 调度 / 远程执行 / K8s 客户端 / 可观测性}}**：
  {{选定技术}}
  - 触发信号：{{mock 里的具体证据，如「指标趋势图」「实时进度条」「cron 配置面板」}}
  - 理由：{{一句话，落到本项目的实际需要}}

## 四、显式排除（刻意没用什么）

<!--
这一节是确认单的灵魂——它替 SDD 划清了边界，让 SDD 不用再去考虑被排除的方向。
列出那些「常见但本项目刻意不引入」的组件，每条写明：排除什么 → 为什么排除。
排除理由要具体到「mock 里没有 X 信号」或「Y 方案已经够用」，不能写「暂时不需要」。
-->

- **不引入 {{组件}}**：{{为什么排除，例：mock 里没有热点读场景，单库查询足够}}
- **不引入 {{组件}}**：{{为什么排除，例：没有跨服务事件，异步任务用进程内 worker 即可}}
- **不引入 {{组件}}**：{{为什么排除}}

## 五、待确认的开放项

<!--
mock 信息不足、或需要用户拍板的点，逐条列出。能给默认建议就给。
开放项越具体越好——「实时刷新用轮询还是 SSE」比「推送方式待定」有用得多。
如果项目形态或开发语言未由用户确认，它们必须列为前两项。
全部确认完成后才能进入 SDD 确认边界；否则本文件只能作为 draft 草案。
-->

- **{{开放项 1}}**：{{背景一句话}}。建议：{{默认推荐 + 何时切换}}
- **{{开放项 2}}**：{{背景一句话}}。建议：{{默认推荐 + 何时切换}}

## 六、一句话交接

<!--
固定句式，把球交给 SDD。
-->

> 本项目定为 **{{形态}}** + **{{语言}}** + **{{关键技术，3-5 个最重要的}}**，
> 刻意排除了 **{{显式排除中最关键的 2-3 个}}**；
> 待用户确认 **{{开放项中最关键的 1-2 个}}** 后，即可进入 SDD 确认边界。

<!--
如果仍有关键开放项未确认，不要写「即可进入 SDD 确认边界」。
改写为：
> 本草案建议定为 **{{形态}}** + **{{语言}}** + **{{关键技术}}**，
> 刻意排除了 **{{显式排除}}**；
> 需先确认 **{{开放项}}**，再进入 SDD 确认边界。
-->
