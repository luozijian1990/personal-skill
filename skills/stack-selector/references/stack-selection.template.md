# 选型确认单：{{project_name}}

<!--
落盘路径：
  docs/plans/YYYY-MM-DD-<topic>-stack-selection.md
  docs/plans/YYYY-MM-DD-<topic>-stack-selection.draft.md（语言、组件或关键约束未确认时）

使用约定：
- 替换全部 {{...}} 占位符。
- 没有内容的小节写「（无）」或「不适用」，不要删除必答小节。
- 每条理由必须落到本项目证据，不能只写「主流 / 性能好 / 生态成熟」。
- 每个维度最多一个推荐方案和一个备选方案。
- 关键开放项未确认时只能写 draft，不能宣告进入 SDD。
-->

## 项目与运行约束

- **核心功能**：{{从 mock / 描述提取的能力清单；注明它们是候选信号}}
- **开发语言**：{{Python / Go}}{{已确认 / 建议，待确认}} —— {{项目证据}}
- **运行位置**：{{本机 / 普通服务器 / Docker / Kubernetes}}
- **规模与任务时长**：{{用户数、数据量、典型和最长任务时长}}
- **外部系统**：{{远程主机、Kubernetes、内部 API；没有则写无}}
- **可复用基础设施**：{{PostgreSQL / MySQL / Redis / Prometheus / SSO 等}}
- **失败处理要求**：{{人工重试 / 自动重试 / 重启恢复 / 审计 / 审批}}

## 系统组件组成

<!-- 组件可组合；同时说明进程内模块还是独立部署。默认模块化单体 Backend。 -->

- **Backend**：{{使用 / 不使用}}；{{职责}}；{{进程和部署方式}}
- **Worker / Scheduler**：{{使用 / 不使用}}；{{职责}}；{{进程内 / 独立部署及理由}}
- **Agent**：{{使用 / 不使用}}；{{职责、部署目标及引入证据}}
- **CLI**：{{使用 / 不使用}}；{{使用者和职责}}
- **Kubernetes Controller / Operator**：{{使用 / 不使用}}；{{是否存在持续协调 / CRD 证据}}

## 最小架构方案

<!-- 只描述组件关系，不要求复杂架构图。 -->

```text
{{例如：React -> Backend -> Worker -> SSH / Kubernetes API
                    |
                    +-> PostgreSQL}}
```

- **部署单元**：{{当前实际需要部署哪些进程或容器}}
- **关键调用链**：{{一次主要用户操作如何经过组件并落到数据或外部系统}}

## 核心技术选型

| 维度 | 推荐方案 | 项目理由 | 备选及切换条件 |
|---|---|---|---|
| Web / CLI / Agent 框架 | {{技术}} | {{实际证据}} | {{最多一个备选；没有则写无}} |
| 数据访问 | {{ORM / SQL / 不适用}} | {{实际证据}} | {{切换条件}} |
| 配置 | {{方案}} | {{实际证据}} | {{切换条件}} |
| 认证与授权 | {{现有 SSO / 网关 / Session / 其他}} | {{实际证据}} | {{切换条件}} |
| 其他核心维度 | {{方案}} | {{实际证据}} | {{切换条件}} |

## 数据与任务执行方案

- **业务数据**：{{数据库、数据边界和选择理由}}
- **时序数据**：{{关系库 / 时序库 / 不适用；数据量、保留时间和查询依据}}
- **任务执行**：{{同步 / 进程内后台任务 / 独立 Worker}}
- **任务状态**：{{如何持久化状态、错误和操作者}}
- **调度**：{{不使用 / 进程内 Scheduler / 独立 Scheduler；重复触发处理}}
- **进度与日志刷新**：{{轮询 / SSE / WebSocket；升级依据}}
- **取消与恢复**：{{取消点、失败处理、重启后状态}}
- **结果保留**：{{结果和执行日志的保留期限及清理方式}}

## 安全与运行基线

<!-- 每项都必须说明处理方式；不适用也要写原因。 -->

- **配置和 Secret**：{{保存、注入、轮换和脱敏方式}}
- **健康检查与优雅退出**：{{存活、就绪、停止接单和退出超时}}
- **结构化日志与错误记录**：{{字段、request/task ID、错误上下文}}
- **超时、重试和并发限制**：{{外部调用、任务及目标级限制}}
- **幂等与重复提交**：{{幂等键、唯一约束或状态机}}
- **数据库迁移与备份**：{{迁移工具、备份周期、恢复方式}}
- **操作审计**：{{操作者、目标、动作、时间、结果}}
- **长任务取消与状态恢复**：{{处理方式或不适用理由}}
- **远程命令白名单**：{{动作模板和参数校验；不适用理由}}
- **凭证保存**：{{SSH、Token、Kubernetes 凭证；不适用理由}}
- **结果和日志保留**：{{期限、容量上限、清理或归档}}

## 显式排除

<!-- 排除理由可来自 mock、运行约束或现有基础设施，不限于页面证据。 -->

- **不引入 {{组件或技术}}**：{{当前证据不足，或更轻方案已经够用}}
- **不引入 {{组件或技术}}**：{{具体理由}}
- **不引入 {{组件或技术}}**：{{具体理由}}

## 后续升级触发条件

- **拆独立 Worker**：{{例如任务阻塞 API、需重启恢复或独立扩缩容时}}
- **引入 Redis / 任务队列**：{{例如任务必须持久化、自动重试或多实例消费时}}
- **引入共享缓存**：{{例如多实例出现可测量热点且数据库成为瓶颈时}}
- **引入时序数据库**：{{例如数据量、保留周期和查询范围超过关系库能力时}}
- **升级实时通信**：{{例如轮询延迟不可接受时升级 SSE；双向高频时再升级 WebSocket}}
- **其他**：{{与本项目直接相关的触发条件；没有则写无}}

## 待确认问题

- **{{问题 1}}**：{{为什么影响架构}}。推荐默认：{{默认值}}。
- **{{问题 2}}**：{{为什么影响架构}}。推荐默认：{{默认值}}。

## 一句话交接

<!-- 最终单固定表达；draft 必须改成「确认后再进入 SDD」。 -->

> 本项目采用 **{{系统组件组合}}**，以 **{{最小架构摘要}}** 运行；核心技术为
> **{{3-5 个关键技术或现有基础设施}}**，安全与运行基线按本单执行；当前明确排除
> **{{2-3 个关键排除项}}**，达到 **{{最关键升级触发条件}}** 时再升级，可进入 SDD 确认边界。

<!-- draft 写法：
> 本草案建议采用 **{{系统组件组合}}** 和 **{{最小架构摘要}}**；需先确认
> **{{关键开放项}}**，再进入 SDD 确认边界。
-->
