# 选型确认单：远程主机执行平台

- **阶段**：DRAFT
- **React Mock / 功能基准**：选择主机、提交命令、查看进度与日志
- **Grill 决策来源**：待执行

## UI 能力证据

- 页面暴露远程执行、任务状态和日志查看能力，但不能单独决定 Worker、Agent 或通信协议。

## 暂定建议

- Go Backend；是否拆独立 Worker、是否部署 Agent、日志采用轮询或 SSE，均待运行约束确认。

## Grill 输入

- 确认主机数量、网络可达性、任务时长、安全隔离、命令白名单、凭证、并发、失败恢复和现有基础设施。

## 一句话交接

完成 Grill 后再 Finalize；当前不得进入 SDD。
