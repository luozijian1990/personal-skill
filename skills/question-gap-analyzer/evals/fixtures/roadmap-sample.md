# Kubernetes 调度与稳定性

## 调度基础

调度器根据资源请求、节点约束和优先级选择节点。资源请求不准确会导致调度判断与真实负载脱节。

## 优先级与抢占

高优先级 Pod 在没有可用节点时可能触发抢占。抢占结果还会受亲和性、终止宽限期和 PodDisruptionBudget 等条件影响，不能把“配置了 PriorityClass”等同于一定能够立即调度。

## 调度失败排查

排查 Pending Pod 时，需要结合调度事件、资源请求、污点容忍、亲和性、拓扑约束和抢占候选结果建立假设，不能只看节点剩余 CPU。
